/**
 * @desc collpase transition component
 */

import xoCollpaseTransition from './collapse-transition';

xoCollpaseTransition.install = (Vue) => {
  Vue.component(xoCollpaseTransition.name, xoCollpaseTransition);
};

export default xoCollpaseTransition;
